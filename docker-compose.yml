name: dynamuszap
services:
  dynamuszap-api:
    container_name: dynamuszap-api
    build: .
    ports:
      - "${PORT:-3333}:${PORT:-3333}"
    volumes:
      - .:/app
      - /app/node_modules
      - ./tokens:/app/tokens
    env_file:
      - .env
    restart: unless-stopped